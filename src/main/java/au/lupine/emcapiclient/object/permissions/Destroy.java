package au.lupine.emcapiclient.object.permissions;

import com.google.gson.JsonArray;

public class Destroy extends TownyAction {
    public Destroy(JsonArray jsonArray) {
        super(jsonArray);
    }
}
