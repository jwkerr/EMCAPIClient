package au.lupine.emcapiclient.object.wrapper.permissions;

import com.google.gson.JsonArray;

public class Build extends TownyAction {
    public Build(JsonArray jsonArray) {
        super(jsonArray);
    }
}
